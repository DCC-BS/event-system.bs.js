{
    "name": "@dcc-bs/event-system.bs.js",
    "version": "1.4.3",
    "description": "Contains a Command bus for handling commands",
    "license": "MIT",
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/types.d.mts",
            "import": "./dist/module.mjs",
            "require": "./dist/module.mjs"
        }
    },
    "main": "./dist/module.mjs",
    "types": "./dist/types.d.mts",
    "files": [
        "dist"
    ],
    "scripts": {
        "prepack": "bunx nuxt-module-build build",
        "generate": "bunx nuxi generate",
        "dev": "bunx nuxi dev playground",
        "dev:build": "bunx nuxi build playground",
        "dev:prepare": "bunx nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare playground",
        "release": "bun lint && bun run test && bun prepack && changelogen --release && git push --follow-tags",
        "lint": "biome lint .",
        "check": "biome check --fix .",
        "test": "vitest run",
        "test:watch": "vitest watch",
        "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
    },
    "dependencies": {
        "@nuxt/kit": "^4.4.2"
    },
    "devDependencies": {
        "@nuxt/devtools": "^4.0.0-alpha.3",
        "@nuxt/eslint-config": "^1.15.2",
        "@nuxt/module-builder": "^1.0.2",
        "@nuxt/schema": "^4.4.2",
        "@nuxt/test-utils": "^4.0.0",
        "@types/node": "^25.5.0",
        "@vitest/coverage-v8": "^4.1.1",
        "changelogen": "^0.6.2",
        "eslint": "^10.1.0",
        "nuxt": "^4.4.2",
        "publish-to-git": "^1.2.0",
        "typescript": "~6.0.2",
        "vitest": "^4.1.1",
        "vue-tsc": "^3.2.6",
        "@biomejs/biome": "^2.4.8"
    }
}
